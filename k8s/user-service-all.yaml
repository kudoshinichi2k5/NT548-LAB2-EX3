apiVersion: apps/v1
kind: Deployment
metadata:
  name: cookmate-user
  namespace: cookmate
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%  
  selector:
    matchLabels:
      app: cookmate-user
  template:
    metadata:
      labels:
        app: cookmate-user
    spec:
      containers:
      - name: cookmate-user
        # Jenkins replaces this line using sed with ${USER_IMAGE}
        # Keep the service name present so sed in Jenkinsfile matches it
        # image: REPLACE_REGISTRY/nt548-project/user-service:latest
        image: kiennlt/cookmate-user:v2
        imagePullPolicy: Always
        ports:
        - containerPort: 5001
        envFrom:
        - secretRef:
            name: cookmate-user-secret
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 5001
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: cookmate-user
  namespace: cookmate
spec:
  type: ClusterIP
  selector:
    app: cookmate-user
  ports:
    - port: 80
      targetPort: 5001